server:
  port: 8091

spring:
  application:
    name: eurekaClientService-1

eureka:
  instance:
    prefer-ip-address: true # ?????? ??? ???? ???? ?? ??
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8181/eureka/
